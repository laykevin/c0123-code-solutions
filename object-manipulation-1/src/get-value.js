/* exported getValue */
function getValue(object, key) {
  return object[key];
}

// Define a function named getvalue that accepts two arguments 'object' (an object) and 'key' (a string).
//   Return the value of the 'key' property of the 'object' object.

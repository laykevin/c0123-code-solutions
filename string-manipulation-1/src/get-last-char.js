/* exported getLastChar */
function getLastChar(string) {
  return string[string.length - 1];
}

// Define a function name getLastChar that accepts one argument, 'string' that is a string.
// return the value of the last index of the argument.

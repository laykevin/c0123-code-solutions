/* exported isLowerCased */
function isLowerCased(word) {
  return word === String(word).toLowerCase();
}

// Define a function named isLowerCased that accepts one arguement, 'word', that is a String.
// compare the arguement 'word' to be strictly equal to value of 'word' in all lower case and return the boolean value.

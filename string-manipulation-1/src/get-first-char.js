/* exported getFirstChar */
function getFirstChar(string) {
  return string[0];
}

// Define a function named getFirstChar, that accepts one arguement 'string', that is a sting.
// return the first letter of the argument.

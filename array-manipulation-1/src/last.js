/* exported last */
function last(array) {
  return array[array.length - 1];
}

// Define a function named last which takes one arguement 'array', that is an array.
// Return the last element of the array.
